import 'package:blithepay/core/constants/app_spacing.dart';
import 'package:flutter/widgets.dart';

// Reusable SizedBox variants - as you requested, even SizedBox should be reusable

class SpaceXs extends SizedBox {
  const SpaceXs({super.key})
      : super(
          width: AppSpacing.xs,
          height: AppSpacing.xs,
        );
}

class SpaceSm extends SizedBox {
  const SpaceSm({super.key})
      : super(
          width: AppSpacing.sm,
          height: AppSpacing.sm,
        );
}

class SpaceMd extends SizedBox {
  const SpaceMd({super.key})
      : super(
          width: AppSpacing.md,
          height: AppSpacing.md,
        );
}

class SpaceBase extends SizedBox {
  const SpaceBase({super.key})
      : super(
          width: AppSpacing.base,
          height: AppSpacing.base,
        );
}

class SpaceLg extends SizedBox {
  const SpaceLg({super.key})
      : super(
          width: AppSpacing.lg,
          height: AppSpacing.lg,
        );
}

class SpaceXl extends SizedBox {
  const SpaceXl({super.key})
      : super(
          width: AppSpacing.xl,
          height: AppSpacing.xl,
        );
}

class SpaceXxl extends SizedBox {
  const SpaceXxl({super.key})
      : super(
          width: AppSpacing.xxl,
          height: AppSpacing.xxl,
        );
}

// Horizontal spacing
class HSpaceXs extends SizedBox {
  const HSpaceXs({super.key})
      : super(
          width: AppSpacing.xs,
        );
}

class HSpaceSm extends SizedBox {
  const HSpaceSm({super.key})
      : super(
          width: AppSpacing.sm,
        );
}

class HSpaceMd extends SizedBox {
  const HSpaceMd({super.key})
      : super(
          width: AppSpacing.md,
        );
}

class HSpaceBase extends SizedBox {
  const HSpaceBase({super.key})
      : super(
          width: AppSpacing.base,
        );
}

class HSpaceLg extends SizedBox {
  const HSpaceLg({super.key})
      : super(
          width: AppSpacing.lg,
        );
}

class HSpaceXl extends SizedBox {
  const HSpaceXl({super.key})
      : super(
          width: AppSpacing.xl,
        );
}

class HSpaceXxl extends SizedBox {
  const HSpaceXxl({super.key})
      : super(
          width: AppSpacing.xxl,
        );
}

// Vertical spacing
class VSpaceXs extends SizedBox {
  const VSpaceXs({super.key})
      : super(
          height: AppSpacing.xs,
        );
}

class VSpaceSm extends SizedBox {
  const VSpaceSm({super.key})
      : super(
          height: AppSpacing.sm,
        );
}

class VSpaceMd extends SizedBox {
  const VSpaceMd({super.key})
      : super(
          height: AppSpacing.md,
        );
}

class VSpaceBase extends SizedBox {
  const VSpaceBase({super.key})
      : super(
          height: AppSpacing.base,
        );
}

class VSpaceLg extends SizedBox {
  const VSpaceLg({super.key})
      : super(
          height: AppSpacing.lg,
        );
}

class VSpaceXl extends SizedBox {
  const VSpaceXl({super.key})
      : super(
          height: AppSpacing.xl,
        );
}

class VSpaceXxl extends SizedBox {
  const VSpaceXxl({super.key})
      : super(
          height: AppSpacing.xxl,
        );
}
